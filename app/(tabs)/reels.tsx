import { View, Text } from "react-native";
import React from "react";

const reels = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-primary font-bold text-3xl">Reels</Text>
    </View>
  );
};

export default reels;
